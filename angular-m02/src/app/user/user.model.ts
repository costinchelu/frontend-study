// ts feature: type
// type User = {
//   id: string;
//   avatar: string;
//   name: string;
// }

// or we can use an interface
// add export to make it available
export interface User {
  id: string;
  avatar: string;
  name: string;
}
